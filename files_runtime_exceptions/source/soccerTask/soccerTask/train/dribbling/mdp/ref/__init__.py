"""Reference (known-good) configs/MDP implementations.

This package contains a frozen snapshot of dribbling task configs that were
previously known to work correctly. The main task entrypoints can import and
re-export these definitions to avoid accidental drift.
"""
